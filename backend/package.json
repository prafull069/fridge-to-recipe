{
  "name": "fridge-to-recipe-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Tiny backend that proxies the LLM call so the API key never reaches the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
